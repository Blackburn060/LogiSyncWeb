# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - logisync

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://logisync.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_3cf32fe65865455fa3f526ba48c38141 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_ae596fb67fde496e81a38dca8b3e2851 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: logisync.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_3cf32fe65865455fa3f526ba48c38141 }}/logisync-frontend:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'logisync'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_4af31e2486184474b7074b85eb033572 }}
        images: 'logisync.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_3cf32fe65865455fa3f526ba48c38141 }}/logisync-frontend:${{ github.sha }}'